/**
 * Widget typography scale — re-exported from @myek/ui (the canonical token
 * source shared with federated remotes) so host tiles and remote tiles use
 * the same scale by construction. Per-token documentation lives there.
 *
 * Colours stay theme-driven via `useTheme()`; letter-spacing and line-height
 * stay per-widget because they encode intent specific to that tile.
 */
export { widgetTheme } from '@myek/ui';
