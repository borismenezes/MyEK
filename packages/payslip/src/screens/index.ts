// `./screens` expose: the printable payslip document. Mounted by the host's
// PayslipSheet (which supplies the bottom-sheet chrome + share capture).
export { default } from './PayslipDocumentScreen';
