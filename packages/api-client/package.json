{
  "name": "@myek/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "MyEK shared HTTP client for federated remotes — fetch wrapper with the auth token-getter and base URL on globalThis slots (ADR-0022 pattern), so every bundle's copy reads the same auth state. Dependency-free (platform fetch).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" }
}
