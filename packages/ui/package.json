{
  "name": "@myek/ui",
  "version": "0.1.0",
  "private": true,
  "description": "MyEK shared design system — theme tokens + typography, consumed by the host and every remote so federated UI matches. (Slice 1: static light tokens; dynamic useTheme-across-bundles is a follow-up.)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@myek/platform": "*"
  }
}
