{
  "name": "@myek/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "MyEK micro-frontend SDK — single source of truth for Module Federation shared-dependency versions (consumed by the host rspack config and every remote's build). Does NOT enumerate remotes; remote discovery is runtime/catalog-driven.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rspack/remote-config": "./rspack/remote-config.mjs",
    "./rspack/load-env": "./rspack/load-env.mjs"
  }
}
